apiVersion: v1
kind: Service
metadata:
  namespace: game-2048
  name: backend-service
spec:
  selector:
    app: backend
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000